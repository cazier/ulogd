# ulogd


# TODO:
- [ ] Need to refactor all querying to reuse logs (and filter in rust not SQL?)
- [ ] Clean up old entries (probably will do this on the logging side, not the retrieval side)
- [ ] Frontend
